#include <stdio.h>
#include <math.h>
#include <string.h>

int main()
{

    int a, b, sum;

    printf("Digite dois numeros: \n");
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("A soma é..: %d\n", sum);

    return 0;
}